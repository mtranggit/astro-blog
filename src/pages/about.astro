---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const pageTitle = 'About Me';

const identity = {
  firstName: 'Michael',
  country: 'Australia',
  occupation: 'Engineer',
  hobbies: ['photography', 'soccer', 'birdwatching', 'pingpong'],
};

const skills = ['HTML', 'CSS', 'JavaScript', 'React', 'GraphQL', 'AzureDevOps', 'AWS'];

const happy = true;
const finished = false;
const goal = 3;

const skillColor = "navy";
const fontWeight = "bold";
const textCase = "uppercase"
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>{pageTitle}</title>
    <style define:vars={{skillColor, fontWeight, textCase}}>
      h1 {
        color: purple;
        font-size: 4rem;
      }
      .skill {
        /* color: palevioletred; */
        color: var(--skillColor);
        font-weight: var(--fontWeight);
        text-transform: var(--textCase);
      }
    </style>
	</head>
	<body>
    <Header />
		<h1>About me</h1>
    <h2>... and my new site built with Astro</h2>
    <p>I am working throught the Astro's introductory tutorial.</p>
    <p>Here are a few facts about me:</p>
    <ul>
      <li>My name is {identity.firstName}.</li>
      <li>I live in {identity.country} and I work as an {identity.occupation}.</li>
      <li>My hobbies are {identity.hobbies.join(', ')}.</li>
    </ul>
    <p>My skills are:</p>
    <ul>
      {skills.map( skill => (<li class="skill">{skill}</li>))}
    </ul>
    {happy && <p>I am happy to be learning Astro!</p>}

    {finished && <p>I finished this tutorial!</p>}

    {goal === 3 ? <p>My goal is to finish in 3 days.</p> : <p>My goal is not {goal} days.</p>}

    <p>Lorem, ipsum dolor sit amet consectetur adipisicing elit. Facere atque nisi ab quos, quibusdam exercitationem veniam expedita eligendi qui eius accusantium dolore perspiciatis nihil labore praesentium beatae dolorem! Minima, distinctio.</p>

    <Footer />
    <script>
			import "../scripts/menu.js";
		</script>
	</body>
</html>
